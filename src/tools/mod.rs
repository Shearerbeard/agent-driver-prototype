//! Orchestration tool surface ported into the spike crate.
//!
//! Only the data types the ported context and continuation-rendering code
//! reads live here; the rig-typed tool structs stay in the aura crate.

pub mod submit_result;
